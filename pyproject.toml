[project]
name = "remove-pdf-watermark"
version = "1.0.0"
description = "Удаление водяного знака «Created in Master PDF Editor» из PDF-файлов"
requires-python = ">=3.10"
dependencies = ["pikepdf>=9.0.0"]

[project.scripts]
remove-watermark = "remove_watermark:main"
